# noah website — public configuration only. No secrets belong here.
# Only `next dev` reads this, to try canonical/OG URLs against another origin.
# Production always uses https://noah.asherin.com (lib/site.ts).
NEXT_PUBLIC_APP_URL=https://noah.asherin.com
